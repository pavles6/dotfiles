[
  {
    "context": "vim_mode == visual",
    "bindings": {
      "shift-s": ["vim::PushAddSurrounds", {}]
    }
  },
  {
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
      "w": "vim::NextSubwordStart",
      "b": "vim::PreviousSubwordStart",
      "e": "vim::NextSubwordEnd",
      "g e": "vim::PreviousSubwordEnd",
      "/": "buffer_search::Deploy"
    }
  },
  {
    "context": "Editor && edit_prediction && !showing_completions",
    "bindings": {
      "ctrl-f": "editor::AcceptEditPrediction",
      "tab": "editor::Tab"
    }
  },
  {
    "context": "AgentPanel && not_editing || GitPanel && not_editing || ProjectPanel || CollabPanel || OutlinePanel && not_editing || ChatPanel || VimControl || EmptyPane || SharedScreen || MarkdownPreview || KeyContextView",
    "bindings": {
      "ctrl-w shift-l": "workspace::ToggleRightDock",
      "ctrl-w shift-h": "workspace::ToggleLeftDock",
      "space p": "file_finder::Toggle",
      "space g G": "git_panel::ToggleFocus",
      "space g e": "project_panel::ToggleFocus",
      "space g a": "agent::NewTextThread",
      "space g A": "agent::ContinueThread",
      "space g o": "outline::Toggle",
      "space g O": "outline_panel::ToggleFocus",
      "space g f": "pane::DeploySearch",
      "space g x": "zed::Extensions",
      "space g l": "git::Blame",
      "space ,": "tab_switcher::Toggle",
      "space b": "workspace::ToggleLeftDock",
      "space r": "workspace::ToggleRightDock"
    }
  },
  {
    "context": "Editor && vim_mode == visual",
    "bindings": {
      "shift-n": "editor::MoveLineDown",
      "shift-p": "editor::MoveLineUp",
      "ctrl-a": "editor::SelectAll"
    }
  }
]
